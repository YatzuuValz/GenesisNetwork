name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Let a running deploy finish rather than cancelling it mid-publish.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Build static export
        run: npm run build
        env:
          DEPLOY_TARGET: github-pages

      # Static export nests RSC prefetch payloads in directories the client
      # requests dot-flattened; without this every prefetch 404s.
      - run: node scripts/flatten-rsc-payloads.mjs

      # Without this, Pages runs Jekyll and drops the _next/ directory.
      - run: touch out/.nojekyll

      # enablement:true provisions the Pages site over the API if it doesn't
      # exist yet, so the first deploy can't lose the race with the UI toggle.
      - uses: actions/configure-pages@v5
        with:
          enablement: true

      - uses: actions/upload-pages-artifact@v3
        with:
          path: out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
